-- Seed data for local development
-- Note: In local dev, Supabase Auth creates users separately.
-- This seed assumes a test user has been created via Auth with a known UUID.
-- You can create one via the Supabase Dashboard at http://localhost:54323

-- Example: Insert test profile (replace UUID after creating a test user)
-- insert into profiles (id, display_name, current_level, ui_language, llm_provider)
-- values ('00000000-0000-0000-0000-000000000001', 'Test User', 'A1', 'en', 'deepseek');
